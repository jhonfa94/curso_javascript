"use strict"

var edad = 35;

var cantidad = "100";

/* convertimos el numero que esta en formato string a tipo number */
var nuevaCantidad = Number(cantidad);

/* PODEMOS CONVERTIR ENTEROS Y DOBLES CON LAS SIGUIENTES FUNCIONES
 */

parseInt(); //enteros
parseFloat(); //numeros con decimales